{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "node",
          "request": "attach",
          "name": "Attach Debug",
          "skipFiles": [
              "<node_internals>/**"
          ],
          "processId": "${command:PickProcess}",
          "outFiles": [
              "${workspaceFolder}/**/*.js"
          ]
      }
  ]
}