/** Códigos de erros relacionados aos seus respectivos módulos*/
export enum ExceptionCodes {
  /** user-registers */
  EMAIL_ALREADY_REGISTERED = 1,
  USERNAME_ALREADY_REGISTERED = 2,
  DATABASE_ERROR = 666,
  DatabaseError
}
